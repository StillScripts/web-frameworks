---
import PreactComponents from "./preact/PreactComponents"
import ReactComponents from "./react/ReactComponents"
import SolidComponents from "./solid/SolidComponents"
import SvelteComponents from "./svelte/SvelteComponents.svelte"
import VueComponents from "./vue/VueComponents.vue"
import type { Components, Framework } from "../utils/components"

interface Props {
  component: Components
  framework: Framework
}

const { component, framework } = Astro.props
---

<div class="mt-4 p-5 inline-block border border-gray-200 shadow-lg rounded-lg">
  {
    framework === "React" && (
      <ReactComponents currentComponent={component} client:visible />
    )
  }
  {
    framework === "Solid" && (
      <SolidComponents currentComponent={component} client:visible />
    )
  }
  {
    framework === "Svelte" && (
      <SvelteComponents currentComponent={component} client:visible />
    )
  }
  {
    framework === "Vue" && (
      <VueComponents currentComponent={component} client:visible />
    )
  }
  {
    framework === "Preact" && (
      <PreactComponents currentComponent={component} client:visible />
    )
  }
</div>
